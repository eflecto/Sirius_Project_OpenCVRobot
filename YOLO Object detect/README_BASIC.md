# 🎯 Базовый скрипт определения расстояния до людей

Простой и понятный скрипт для определения расстояния до людей с помощью YOLO8 и калиброванной камеры.

## 📋 Что делает скрипт

1. ✅ Загружает калибровку камеры из `camera_calib_result.npz`
2. ✅ Детектирует людей с помощью YOLO8
3. ✅ Вычисляет расстояние **двумя способами**:
   - По ширине плеч (45 см)
   - По высоте человека (170 см)
4. ✅ Использует **среднее арифметическое** (твоя идея!)
5. ✅ Вычисляет **угол** относительно центра камеры
6. ✅ Показывает результаты в реальном времени
7. ⚠️ **БЕЗ коррекции дисторсии** (добавим позже)

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install opencv-python ultralytics numpy
```

### 2. Подготовка файлов

Положите в одну папку:
- `yolo_distance_basic.py` - этот скрипт
- `camera_calib_result.npz` - ваш файл калибровки

### 3. Запуск

```bash
python yolo_distance_basic.py
```

---

## 🎮 Управление

| Клавиша | Действие |
|---------|----------|
| **ESC** | Выход из программы |
| **S** | Сохранить текущий кадр |
| **P** | Пауза / Продолжить |

---

## 📐 Как это работает

### Формулы расстояния:

```python
# 1. Расстояние по ширине плеч
dist_width = (45 см × fx) / bbox_width

# 2. Расстояние по высоте
dist_height = (170 см × fy) / bbox_height

# 3. Среднее арифметическое (финальное расстояние)
distance = (dist_width + dist_height) / 2
```

### Формула угла:

```python
# Смещение от центра камеры
offset_x = bbox_center_x - cx

# Угол в градусах
angle = arctan(offset_x / fx) × (180 / π)

# + справа, - слева от центра
```

### Что измеряем:

```
         📷 Камера/Робот
         |
         | distance_z ← ЭТО вычисляет скрипт
         |              (расстояние вдоль оси камеры)
         |
         +─────────  angle
         |\
         | \
         |  \ distance_real ← реальное расстояние
         |   \                (гипотенуза)
         |    \
         |     👤 Человек
```

---

## 📊 Что показывает на экране

### Для каждого человека:
- **Person #N (0.95)** - номер и уверенность детекции
- **Dist: 4.52m** - расстояние вдоль оси камеры (Z)
- **Angle: +15.3deg** - угол (+справа, -слева)

### Общая информация:
- **FPS** - скорость обработки
- **People: 2** - количество людей в кадре
- **Closest: 3.2m @ +10°** - ближайший человек

### Цветовая индикация:
- 🔴 **Красный** (< 1.5м) - VERY CLOSE!
- 🟠 **Оранжевый** (1.5-3м) - Close
- 🟡 **Желтый** (3-5м) - средне
- 🟢 **Зеленый** (> 5м) - далеко

---

## 🔧 Настройки

### Изменить параметры человека:

```python
# В __init__ класса PersonDistanceDetector
self.PERSON_HEIGHT = 170  # см - измените для детей/высоких людей
self.PERSON_WIDTH = 45    # см - средняя ширина плеч
```

### Использовать другую модель YOLO:

```python
# В функции main()
detector = PersonDistanceDetector(
    model_path='yolov8s.pt'  # или yolov8m.pt, yolov8l.pt
)
```

Модели по скорости и точности:
- `yolov8n.pt` - самая быстрая ⚡⚡⚡ (рекомендуется для начала)
- `yolov8s.pt` - средняя ⚡⚡
- `yolov8m.pt` - точная ⚡
- `yolov8l.pt` - самая точная, но медленная

### Изменить порог детекции:

```python
# В методе detect_people
def detect_people(self, frame, conf_threshold=0.5):  # 0.5 = 50%
    ...
```

---

## 📈 Точность измерений

### Ожидаемая точность (БЕЗ коррекции дисторсии):

| Расстояние | Точность | Примечание |
|------------|----------|------------|
| 1-2 метра | ±20-30 см | Близко |
| 2-4 метра | ±30-50 см | Средне |
| 4-7 метров | ±50-80 см | Далеко |

### Факторы влияющие на точность:

✅ **Улучшают точность:**
- Человек фронтально к камере
- Хорошее освещение
- Человек в центре кадра (угол < 15°)
- Рост близок к 170 см

⚠️ **Ухудшают точность:**
- Человек боком (особенно по ширине)
- Дети / очень высокие люди
- Человек по краям кадра (дисторсия)
- Плохое освещение (YOLO ошибается)

---

## 🤖 Использование для робота

### Получение данных для навигации:

```python
# После детекции
detections = detector.detect_people(frame)

if detections:
    # Ближайший человек
    closest = detections[0]
    
    distance = closest['distance_z']      # Расстояние (м)
    angle = closest['angle_deg']          # Угол (градусы)
    offset_x = closest['offset_x']        # Смещение влево/вправо (м)
    
    # Команды роботу
    robot.rotate(angle)        # Повернись на угол
    robot.move_forward(distance)  # Проедь distance метров
    
    # ИЛИ
    robot.navigate_to_polar(distance, angle)
```

### Полярные координаты (для робота):

```python
# Что вычисляет скрипт:
distance_z = 5.0 м     # Расстояние вдоль оси камеры
angle = +15°           # Угол вправо

# Простая навигация:
1. robot.turn(15°)     # Повернись направо
2. robot.forward(5.0)  # Проедь вперёд
3. Приехал к человеку! ✅
```

---

## 🔍 Структура кода

```python
class PersonDistanceDetector:
    __init__()                    # Инициализация, загрузка калибровки
    calculate_distance_and_angle()  # Вычисление расстояния и угла
    detect_people()               # Детекция YOLO
    draw_detections()             # Визуализация
    run_camera()                  # Главный цикл
```

### Ключевые методы:

#### `calculate_distance_and_angle(bbox)`
Принимает bbox, возвращает:
```python
{
    'distance_z': 4.52,        # Расстояние вдоль оси (м)
    'distance_real': 4.68,     # Реальное расстояние (м)
    'angle_deg': +15.3,        # Угол (градусы)
    'offset_x': 1.23,          # Смещение (м)
    'dist_by_width': 4.20,     # По ширине (м)
    'dist_by_height': 4.85     # По высоте (м)
}
```

---

## ❓ Частые вопросы

### Q: Почему расстояние неточное?

**A:** Возможные причины:
1. Человек не фронтально (особенно боком)
2. Рост сильно отличается от 170 см
3. Нужна коррекция дисторсии (добавим позже)
4. YOLO неточно детектирует bbox

### Q: В чём разница distance_z и distance_real?

**A:**
```
distance_z = расстояние вдоль оси камеры (Z-координата)
distance_real = реальное расстояние до человека (гипотенуза)

Разница зависит от угла:
• Угол 0-10° → разница < 2% (можно игнорить)
• Угол 10-20° → разница 2-6% (малая)
• Угол > 20° → разница > 6% (значительная)
```

### Q: Как улучшить точность?

**A:**
1. Добавить коррекцию дисторсии (следующий шаг!)
2. Использовать более точную модель YOLO
3. Добавить сглаживание по времени
4. Калибровать параметры под конкретных людей

### Q: Можно ли использовать для детей?

**A:** Да, но нужно изменить параметры:
```python
self.PERSON_HEIGHT = 130  # см для детей
self.PERSON_WIDTH = 35    # см
```

---

## 📝 Следующие шаги

### Что добавим далее:

1. ✅ **Коррекция дисторсии** - улучшит точность на 5-10%
2. ✅ **Трекинг людей** - уникальные ID для каждого человека
3. ✅ **Сглаживание** - стабильные измерения по времени
4. ✅ **Адаптивные параметры** - автоопределение детей/взрослых
5. ✅ **3D координаты** - полная позиция (X, Y, Z)

---

## 🐛 Отладка

### Скрипт не запускается:

```bash
# Проверьте зависимости
pip install opencv-python ultralytics numpy

# Проверьте наличие файла калибровки
ls -la camera_calib_result.npz
```

### Камера не открывается:

```python
# Попробуйте другой ID камеры
detector.run_camera(camera_id=1)  # или 2, 3...
```

### FPS слишком низкий:

```python
# Используйте более легкую модель
detector = PersonDistanceDetector(model_path='yolov8n.pt')

# Уменьшите разрешение камеры (добавьте после cap = cv2.VideoCapture)
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)
```

---

## 📄 Лицензия

MIT License

---

## 🙏 Благодарности

- YOLO8 by Ultralytics
- OpenCV
- Ваша калибровка камеры! 🎯

---

**Готово к использованию!** 🚀

Запускайте скрипт, тестируйте и присылайте фидбек для улучшений!
