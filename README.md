# Система детектирования человека по цвету одежды для робота

## 🔧 Исправления проблем
Все скрипты обновлены для исправления ошибок с типами данных numpy и OpenCV.

## 📁 Файлы проекта

### 1. minimal_test.py - Самый простой тест
**Для быстрой проверки работоспособности**
```bash
python minimal_test.py
```
- Кликните мышкой на одежду для захвата цвета
- Моментально видите результат детектирования
- ESC для выхода

### 2. simple_clothing_tracker.py - Простой трекер
**Быстрый старт с автозахватом**
```bash
python simple_clothing_tracker.py
```
- Поместите человека в центр кадра
- Нажмите ПРОБЕЛ для захвата цвета
- Система начнет отслеживание с визуализацией траектории

### 3. robust_clothing_tracker.py - Улучшенный трекер
**Адаптивный и устойчивый к помехам**
```bash
python robust_clothing_tracker.py
```
- ПРОБЕЛ - захват цвета
- +/- регулировка чувствительности
- Автоматическая адаптация диапазонов
- Обработка потери цели

### 4. clothing_color_capture.py - Калибровка
**Точная настройка параметров**
```bash
python clothing_color_capture.py
```
- Интерактивная настройка ROI
- Регулировка HSV диапазонов
- Сохранение параметров в JSON

### 5. clothing_tracker.py - Полная система
**Производственная версия с Калманом**
```bash
# Сначала калибровка
python clothing_color_capture.py
# Затем запуск
python clothing_tracker.py
```

## 🚀 Рекомендуемый порядок тестирования

1. **Начните с minimal_test.py** - проверьте, что камера работает и цвета детектируются
2. **Попробуйте simple_clothing_tracker.py** - базовое отслеживание
3. **Используйте robust_clothing_tracker.py** - если нужна устойчивость
4. **Для точной настройки используйте clothing_color_capture.py + clothing_tracker.py**

## 🎯 Алгоритм работы

### Этап 1: Захват цвета
- Выделение области интереса (ROI) с одеждой
- Анализ цвета в HSV пространстве
- Определение диапазонов для фильтрации

### Этап 2: Детектирование
```python
# Бинаризация по цвету
mask = cv2.inRange(hsv_frame, lower_bound, upper_bound)
# Морфологические операции для очистки
mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, kernel)
mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, kernel)
```

### Этап 3: Отслеживание
- Поиск контуров
- Фильтрация по размеру и форме
- Вычисление центра масс
- Оценка расстояния: `distance = (real_width * focal_length) / pixel_width`

### Этап 4: Координаты для робота
```python
x_robot = distance * sin(angle)
y_robot = distance * cos(angle)
theta = angle_to_target
```

## ⚙️ Параметры для настройки

### HSV диапазоны (по умолчанию):
- **H (Hue)**: ±15-25 (цветовой тон)
- **S (Saturation)**: ±50-80 (насыщенность)
- **V (Value)**: ±50-80 (яркость)

### Калибровка камеры:
```python
focal_length = 500-600  # Зависит от камеры
person_width_cm = 45-50  # Средняя ширина торса
```

## 📊 Выходные данные для робота

Каждый трекер выдает:
- **x_robot** - смещение влево/вправо (метры)
- **y_robot** - расстояние вперед (метры)
- **theta** - угол поворота (радианы)

## 🐛 Решение проблем

### Если не детектирует цвет:
1. Увеличьте диапазоны HSV
2. Проверьте освещение
3. Используйте robust_clothing_tracker.py с адаптивными диапазонами

### Если много ложных срабатываний:
1. Уменьшите диапазоны HSV
2. Увеличьте минимальную площадь контура
3. Добавьте фильтр по соотношению сторон

### Если теряет цель при движении:
1. Используйте clothing_tracker.py с фильтром Калмана
2. Увеличьте max_lost_frames
3. Расширьте диапазон V (яркость)

## 📝 Примечания

- Все скрипты работают с OpenCV 4.x
- Требуется Python 3.6+
- Работает в реальном времени на слабом железе
- Не требует GPU или нейросетей

## 🎨 Советы по выбору одежды для тестирования

Лучше всего работает с:
- Яркими однотонными цветами
- Контрастной относительно фона одеждой
- Матовыми тканями (не блестящими)

Избегайте:
- Камуфляжа или сложных узоров
- Одежды похожего с фоном цвета
- Блестящих или отражающих материалов
